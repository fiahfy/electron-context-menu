import { open } from '../src'

describe('open', () => {
  test('should call', () => {
    // TODO: https://github.com/facebook-atom/jest-electron-runner/issues/73
    // expect(() => {
    //   open()
    // }).not.toThrow(Error)
    // expect(() => {
    //   open([{ role: 'copy' }])
    // }).not.toThrow(Error)
    // expect(() => {
    //   open([], { hidden: false })
    // }).not.toThrow(Error)
    // expect(() => {
    //   open([], { label: 'foo' })
    // }).not.toThrow(Error)
  })
})
